# Q5 — just right (and)

temp = float(input("Current temperature is >> "))

if temp >= 15 and temp <= 25:
    print("Just right for reading outside.")
else:
    print("Not the best for reading outside.")