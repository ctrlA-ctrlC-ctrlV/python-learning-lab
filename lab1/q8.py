# Q8 — the `input` trap (input always returns a `str`)

first_num = input("First number >> ");
second_num = input("Second_num number >> ");

# Glued together
print(first_num+second_num)
# Sum
print(int(first_num)+int(second_num));